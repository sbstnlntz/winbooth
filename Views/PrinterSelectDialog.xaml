<Window x:Class="FotoboxApp.Views.PrinterSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Drucker auswählen"
        Height="520"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Drucker auswählen"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1"
                   Text="Tippe den Drucker an, der im User-Menü angeboten werden soll."
                   Foreground="#666"
                   Margin="0,0,0,18"
                   FontSize="16"
                   TextWrapping="Wrap"/>

        <ListBox x:Name="ItemsList"
                 Grid.Row="2"
                 ItemsSource="{Binding Options}"
                 SelectionMode="Single"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionChanged="ItemsList_SelectionChanged">
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White"
                            CornerRadius="12"
                            BorderBrush="#DDDDDD"
                            BorderThickness="1"
                            Padding="16,14">
                        <RadioButton Content="{Binding Name}" GroupName="PrinterOptions" FontSize="20" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Checked="DeviceRadio_Checked">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="IsEnabled" Value="True"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Name}" Value="Keine Drucker gefunden">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Setter Property="Foreground" Value="#888"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Abbrechen"
                    Width="140"
                    Height="46"
                    Margin="0,0,14,0"
                    FontSize="18"
                    Padding="12,4"
                    Click="Cancel_Click"/>
            <Button Content="Übernehmen"
                    Width="160"
                    Height="46"
                    FontSize="18"
                    Padding="12,4"
                    IsDefault="True"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>


