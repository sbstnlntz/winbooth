<!-- Checklist dialog for choosing multiple hardware devices. -->
<Window x:Class="winbooth.Views.DeviceMultiSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Geräte auswählen"
        Height="520"
        Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleLabel"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,6"
                   TextWrapping="Wrap"/>

        <TextBlock Grid.Row="1"
                   Text="Tippe die Geräte an, die im User-Menü verfügbar sein sollen."
                   Foreground="#666"
                   Margin="0,0,0,18"
                   FontSize="16"
                   TextWrapping="Wrap"/>

        <ListBox x:Name="ItemsList"
                 Grid.Row="2"
                 ItemsSource="{Binding Options}"
                 SelectionMode="Extended"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White"
                            CornerRadius="12"
                            BorderBrush="#DDDDDD"
                            BorderThickness="1"
                            Padding="16,14">
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" FontSize="20" VerticalAlignment="Center">
                        <CheckBox.Style>
                            <Style TargetType="CheckBox" BasedOn="{StaticResource FB_CheckBoxBaseStyle}">
                                <Setter Property="IsEnabled" Value="True"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Name}" Value="Keine Geräte gefunden">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Setter Property="Foreground" Value="#888"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Abbrechen"
                    Width="140"
                    Height="46"
                    Margin="0,0,14,0"
                    FontSize="18"
                    Padding="12,4"
                    IsCancel="True"
                    Click="Cancel_Click"/>
            <Button Content="Speichern"
                    Width="160"
                    Height="46"
                    FontSize="18"
                    Padding="12,4"
                    IsDefault="True"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>

