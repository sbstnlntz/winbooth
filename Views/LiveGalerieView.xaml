<UserControl x:Class="FotoboxApp.Views.LiveGalerieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource FB_BackgroundBrush}">
    <Grid>
        <!-- Hauptebene: Galerie und Schließen-Button -->
        <DockPanel>
            <Button x:Name="CloseButton"
                    Content="✕"
                    DockPanel.Dock="Top"
                    Width="60" Height="60"
                    FontSize="32"
                    Style="{StaticResource FB_ElegantButton}"
                    Background="{StaticResource FB_AccentBrush}"
                    Foreground="{StaticResource FB_ButtonTextBrush}"
                    Margin="10"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Click="CloseButton_Click"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                          Background="{StaticResource FB_BackgroundBrush}">
                <ScrollViewer.Resources>
                    <Style TargetType="ScrollBar">
                        <Setter Property="Opacity" Value="0"/>
                        <Setter Property="Width" Value="0"/>
                    </Style>
                </ScrollViewer.Resources>
                <ItemsControl x:Name="GalleryItems">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10"
                                    Background="{StaticResource FB_BackgroundBrush}">
                                <Image Source="{Binding}" Width="220" Height="160" Stretch="UniformToFill" Cursor="Hand"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>

        <!-- MODAL-OVERLAY: Immer GANZ OBEN (letztes Kind im Grid!) -->
        <Grid x:Name="ModalOverlay"
              Background="{StaticResource FB_OverlayBrush}"
              Visibility="Collapsed"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Panel.ZIndex="1000">
            <Grid>
                <Border
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Padding="16">
                    <Image x:Name="ModalImage"
                           Stretch="Uniform"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           MaxHeight="900" MaxWidth="1600"/>
                </Border>
                <Button Content="✕"
                        Width="100" Height="100"
                        FontSize="48"
                        Style="{StaticResource FB_ElegantButton}"
                        Background="{StaticResource FB_WhiteBrush}"
                        Foreground="{StaticResource FB_AccentBrush}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="40"
                        Click="ModalClose_Click"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
