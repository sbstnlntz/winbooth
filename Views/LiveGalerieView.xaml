<UserControl x:Class="winbooth.Views.LiveGalerieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource FB_BackgroundBrush}">
    <Grid>
        <!-- Hauptebene: Galerie und Schließen-Button -->
        <DockPanel>
            <Button x:Name="CloseButton"
                    Style="{StaticResource FB_DialogCloseButtonStyle}"
                    DockPanel.Dock="Top"
                    Margin="10"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Click="CloseButton_Click"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          PanningMode="VerticalOnly"
                          Padding="12,0,16,0"
                          Background="{StaticResource FB_BackgroundBrush}">
                <ScrollViewer.Resources>
                    <Style x:Key="GalleryScrollBarRepeatButtonStyle" TargetType="RepeatButton">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="RepeatButton">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="IsTabStop" Value="False"/>
                        <Setter Property="Focusable" Value="False"/>
                    </Style>
                    <Style TargetType="ScrollBar">
                        <Setter Property="Width" Value="12"/>
                        <Setter Property="Margin" Value="12,8,4,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid Background="Transparent">
                                        <Border Background="#22000000" CornerRadius="6"/>
                                        <Track x:Name="PART_Track"
                                               Orientation="{TemplateBinding Orientation}"
                                               Minimum="{TemplateBinding Minimum}"
                                               Maximum="{TemplateBinding Maximum}"
                                               Value="{TemplateBinding Value}"
                                               ViewportSize="{TemplateBinding ViewportSize}">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}"
                                                              Style="{StaticResource GalleryScrollBarRepeatButtonStyle}"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}"
                                                              Style="{StaticResource GalleryScrollBarRepeatButtonStyle}"/>
                                            </Track.IncreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Background="#FF0B8FCE">
                                                    <Thumb.Template>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="6"
                                                                    Margin="0,4,0,4"/>
                                                        </ControlTemplate>
                                                    </Thumb.Template>
                                                </Thumb>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Resources>
                <ItemsControl x:Name="GalleryItems"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Top">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel x:Name="GalleryWrapPanel"
                                       Orientation="Horizontal"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="8"
                                    Padding="6"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    CornerRadius="14"
                                    Background="#33000000">
                                <Viewbox Stretch="Uniform"
                                         StretchDirection="DownOnly">
                                    <Grid Width="220" Height="160" ClipToBounds="True">
                                        <Border Background="#22000000" CornerRadius="10"/>
                                        <Image Source="{Binding}"
                                               Stretch="Uniform"
                                               SnapsToDevicePixels="True"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    </Grid>
                                </Viewbox>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>

        <!-- MODAL-OVERLAY: Immer GANZ OBEN (letztes Kind im Grid!) -->
        <Grid x:Name="ModalOverlay"

              Background="{StaticResource FB_OverlayBrush}"

              Visibility="Collapsed"

              HorizontalAlignment="Stretch"

              VerticalAlignment="Stretch"

              Focusable="True"

              Panel.ZIndex="1000">

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="Auto"/>

                <ColumnDefinition Width="*"/>

                <ColumnDefinition Width="Auto"/>

            </Grid.ColumnDefinitions>



            <Button x:Name="PreviousButton"

                    Grid.Column="0"

                    Content="‹"

                    Width="120"

                    Height="120"

                    FontSize="48"

                    Background="#AA000000"

                    Foreground="White"

                    BorderBrush="#66000000"

                    BorderThickness="2"

                    Margin="20"

                    Click="PreviousButton_Click"/>



            <Border Grid.Column="1"

                    HorizontalAlignment="Center"

                    VerticalAlignment="Center"

                    Padding="24">

                <Image x:Name="ModalImage"

                       Stretch="Uniform"

                       VerticalAlignment="Center"

                       HorizontalAlignment="Center"

                       MaxHeight="900"

                       MaxWidth="1600"

                       IsManipulationEnabled="True"

                       ManipulationCompleted="ModalOverlay_ManipulationCompleted"/>

            </Border>



            <Button x:Name="NextButton"

                    Grid.Column="2"

                    Content="›"

                    Width="120"

                    Height="120"

                    FontSize="48"

                    Background="#AA000000"

                    Foreground="White"

                    BorderBrush="#66000000"

                    BorderThickness="2"

                    Margin="20"

                    Click="NextButton_Click"/>



            <Button x:Name="ModalCloseButton"
                    Grid.ColumnSpan="3"
                    Style="{StaticResource FB_DialogCloseButtonStyle}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="32"
                    Click="ModalClose_Click"/>

        </Grid>
    </Grid>
</UserControl>
