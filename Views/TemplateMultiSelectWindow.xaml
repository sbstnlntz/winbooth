<Window x:Class="FotoboxApp.Views.TemplateMultiSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:FotoboxApp.Converters"
        Title="Designs freigeben"
        Height="720"
        Width="960"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        WindowStyle="None">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Designs freigeben"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="#222"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1"
                    Style="{StaticResource FB_DialogCloseButtonStyle}"
                    Click="Cancel_Click"/>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="Wähle die Foto-Designs aus, die im User-Menü angezeigt werden sollen."
                   FontSize="18"
                   Foreground="#555"
                   Margin="0,0,0,20"
                   TextWrapping="Wrap"/>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer" BasedOn="{StaticResource {x:Type ScrollViewer}}">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasTemplates}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding Options}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      Margin="12"
                                      Background="Transparent"
                                      BorderThickness="0">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Card"
                                            CornerRadius="16"
                                            BorderThickness="2"
                                            Padding="12"
                                            Background="White"
                                            BorderBrush="#CCCCCC">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Border Background="#111"
                                                    CornerRadius="12"
                                                    Grid.Row="0">
                                                <Image Source="{Binding Template.PreviewImage}"
                                                       Stretch="Uniform"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Margin="4"/>
                                            </Border>
                                            <StackPanel Grid.Row="1"
                                                        Orientation="Horizontal"
                                                        Margin="0,10,0,0"
                                                        HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding Template.Name}"
                                                           FontSize="18"
                                                           FontWeight="Bold"
                                                           Foreground="#222"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Card" Property="BorderBrush" Value="#FF8600"/>
                                            <Setter TargetName="Card" Property="Background" Value="#FFF7EB"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding Template}" Value="{x:Null}">
                                            <Setter TargetName="Card" Property="Opacity" Value="0.5"/>
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="2"
                   Text="Es wurden noch keine Designs gefunden."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="#777">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasTemplates}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button Content="Auswahl übernehmen"
                    Width="220"
                    Height="48"
                    FontSize="18"
                    Padding="18,6"
                    Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>


