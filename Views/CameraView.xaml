<UserControl x:Class="FotoboxApp.Views.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource FB_BackgroundBrush}">

	<Grid Grid.Row="1">
		<!-- Viewbox mit Bild -->
		<Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
			<Canvas x:Name="TemplateCanvas" Width="1600" Height="1200">
				<!-- Livebild -->
				<Image x:Name="RegionPreviewImage"
					   Stretch="Fill"
					   Visibility="Collapsed"
					   IsHitTestVisible="False"/>
				<!-- Template -->
				<Image x:Name="TemplateBackground"
					   Stretch="None"
					   Canvas.Left="0"
					   Canvas.Top="0"/>
			</Canvas>
		</Viewbox>

		<!-- Countdown -->
		<TextBlock x:Name="CountdownText"
				   Text="5"
				   FontSize="160"
				   FontWeight="Bold"
				   Foreground="White"
				   Background="Transparent"
				   Opacity="0"
				   TextAlignment="Center"
				   VerticalAlignment="Center"
				   HorizontalAlignment="Center"
				   Visibility="Collapsed"
				   FontFamily="{StaticResource FB_FontFamilyAccent}">
			<TextBlock.Effect>
				<DropShadowEffect Color="Black"
							  BlurRadius="20"
							  ShadowDepth="0"
							  Opacity="0.8"/>
			</TextBlock.Effect>
		</TextBlock>

		<!-- Flash-Effekt -->
		<Border x:Name="FlashOverlay"
				Background="White"
				Opacity="0"
				Visibility="Collapsed"/>

		<!-- Review-Overlay -->
		<!-- Review-Overlay -->
		<Grid x:Name="ReviewOverlay"
			  Background="Transparent"
			  Visibility="Collapsed"
			  HorizontalAlignment="Stretch"
			  VerticalAlignment="Stretch"
			  Panel.ZIndex="10">

			<StackPanel HorizontalAlignment="Center"
						VerticalAlignment="Bottom"
						Margin="0,0,0,80">

				<!-- Buttons mit Schatten, aber kein weißer Hintergrund -->
				<Border Background="Transparent"
						CornerRadius="20"
						Padding="20,10">
					<Border.Effect>
						<DropShadowEffect Color="Black"
										  BlurRadius="20"
										  ShadowDepth="4"
										  Opacity="0.5"/>
					</Border.Effect>

					<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
						<Button Click="RepeatPhoto_Click"
								Margin="0,0,20,0"
								Padding="20,10"
								FontSize="22"
								FontWeight="Bold"
								Background="Transparent"
								BorderThickness="0"
								Cursor="Hand">
							<StackPanel Orientation="Horizontal">
								<TextBlock Text="🔁" FontSize="28" Margin="0,0,10,0"/>
								<TextBlock Text="FOTO WIEDERHOLEN" Foreground="Red"/>
							</StackPanel>
						</Button>

						<Button Click="ContinuePhoto_Click"
								Padding="20,10"
								FontSize="22"
								FontWeight="Bold"
								Background="Transparent"
								BorderThickness="0"
								Cursor="Hand">
							<StackPanel Orientation="Horizontal">
								<TextBlock Text="✅" FontSize="28" Margin="0,0,10,0"/>
								<TextBlock Text="WEITER" Foreground="Green"/>
							</StackPanel>
						</Button>
					</StackPanel>
				</Border>
			</StackPanel>
		</Grid>


	</Grid>

</UserControl>


